%h1= @list[:name]
= link_to "Edit", edit_list_path
= link_to "Delete", list_path, :method => :delete,
                               :confirm => "Are you sure?",
                               :title => @list.name